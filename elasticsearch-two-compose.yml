version: '3.3'
services:
  elasticsearch-two:
    container_name: elasticsearch-two
    image: docker.elastic.co/elasticsearch/elasticsearch:6.8.13
    user: elasticsearch
    command: ["sh", "-c", "bin/elasticsearch-plugin list | grep -q discovery-ec2 || bin/elasticsearch-plugin install discovery-ec2 --batch && ./bin/elasticsearch"]
    environment:
      - "ES_JAVA_OPTS=-Xms2g -Xmx2g"
      - node.name=test-es-two
      - cluster.initial_master_nodes=test-es-one,test-es-two,test-es-three
    volumes:
      - esdata-two:/usr/share/elasticsearch/data
      - ./elasticsearch-one-config.yml:/usr/share/elasticsearch/config/elasticsearch.yml # Shared config
      - es-logs-two:/usr/share/elasticsearch/logs
      - ./log4j2.properties:/usr/share/elasticsearch/config/log4j2.properties # Shared log config
    ports:
      - 9200:9200 # Host port for ES node two (assumes this compose runs on a dedicated host)
      - 9300:9300 # Host transport port for ES node two
    restart: always

volumes:
  es-logs-two: {}
  esdata-two:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: /var/enc/db/elasticsearch2/data
